import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.question1.app',
  appName: 'question1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
